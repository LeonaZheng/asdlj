// Preload runs before your web page loads.
// Keep it minimal for safety. You can expose safe APIs here if needed later.
window.addEventListener('DOMContentLoaded', () => {
  // Example: document.body.dataset.electron = "true";
});
